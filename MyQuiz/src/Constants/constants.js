import { Dimensions } from "react-native";

const { width, height } = Dimensions.get("window");

export const SIZES = {
    width,
    height,
};

export const COLORS = {
    primary: "#F3EFE0",
    secondary: "#434242",
    tertiary: "#222222",

    green: "#60993E",
    red: "#C84630",
};
